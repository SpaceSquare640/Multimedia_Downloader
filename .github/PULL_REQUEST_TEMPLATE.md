<!-- Thanks for contributing! Keep this short. -->

## What & why
<!-- What does this change, and why? Link any related issue: "Closes #123". -->

## How was it tested?
<!-- e.g. `python -m unittest discover -s tests`, `npm run check`, manual steps. -->

## Checklist
- [ ] `python -m unittest discover -s tests` passes
- [ ] `npm run check` (svelte-check) passes, if the frontend changed
- [ ] Updated `CHANGELOG.md` / docs if user-facing behavior changed
