# tests/

Phase 1+ deliverable. Unit tests for:

- `engine/` — Downloader / Converter parity with V3.0, batch/queue behaviour.
- `locales/` — every locale has the same key set as `en.json` (no missing keys).
- `ai/` — orchestrator plan structure (Phase 5).
